# atlantis.yaml
version: 3
projects:
- name: dev
  dir: terraform/dev
  workflow: custom
- name: preprod
  dir: terraform/preprod
  workflow: custom
- name: prod
  dir: terraform/prod
  workflow: custom